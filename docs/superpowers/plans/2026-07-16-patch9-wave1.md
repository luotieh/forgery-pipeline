# PATCH 9 Wave 1 实施计划（9.3 split 防泄漏 + 9.4 驱动加固工具层 + 9.5 prereg v3 草案）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 PATCH 9 中纯本地、零 GPU 的 B3 阻断项：`base_id` 字段 + V8–V10 split 级防泄漏校验（注毒负例）、B3 驱动加固的可复用工具层（preflight 断言 + 幂等续跑原语）、PREREG_gate2_v3 草案。9.1/9.2（builder 采样政策）与 9.5 设计冻结 config 归 Wave 2；9.6 由控制器另行内联执行。

**Architecture:** `base_id` 由各 builder 在生成时写入（D0=自身 image_id，衍生行=底图 image_id，vae_rt/postprocess 继承），回填脚本扩展；V8–V10 进 `validate.check_all`（V9/V10 由 CLI `--split-config` 从 `configs/split.yaml` 读 holdout 清单，None 即跳过）；驱动加固拆成 `scripts/b3_preflight.py`（HEAD/净树/磁盘断言）与 `forgery_pipeline/rundir.py`（fsync 追加 + done-marker + 残尾检测）。

**Tech Stack:** 纯 numpy/pydantic/git 子进程；零新依赖；mock 全链 CPU 可测。

## Global Constraints

- 注释/文档中文、标识符 English、确定性；sklearn-free；每任务 TDD（先红后绿）；`python3 -m pytest -q` 全套全绿（基线 **180 passed**）后才 commit；commit message 尾行 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- PATCH 7 语义零改动（io_chain/sample_kind/compositing 不动）；V1–V7 判据不动。
- V9/V10 的 holdout 值域来自唯一 config 源（`configs/split.yaml`），代码零硬编码；`testc_holdout: object_replacement`（PATCH 8.3 裁定值）随本 wave 写死进该 yaml。
- V8/V9/V10 各配**注毒负例单测**（构造违例行 → check_all 返回带对应前缀的消息）+ 绿路径测试。

---

## File Structure

| 文件 | 职责 |
|---|---|
| `src/forgery_pipeline/schema.py` (M) | +`base_id: Optional[str]` |
| `builders/d0_real.py` `d1_whole.py` `d2_local.py` `d3_web.py` `d4_explain.py` `probe.py` (M) | 行写入 base_id（D1 全生成行 base_id=自身 image_id 自成组） |
| `src/forgery_pipeline/pipeline.py` (M) | vae_rt 行继承源 base_id |
| `scripts/backfill_manifest_v7.py` (M) | 回填 base_id（real 行=image_id；衍生行=其 real_image_path 对应 real 行的 image_id，查不到则用 real_image_path 字符串本身作组键） |
| `src/forgery_pipeline/validate.py` (M) | V8（base_id 同组同 split + postprocess 继承）V9（holdout 生成器不入 train/val）V10（testc holdout 算子不入 train/val）；`check_all(..., holdout_generators=None, testc_holdout=None)` |
| `src/forgery_pipeline/cli.py` (M) | validate-manifest 增 `--split-config`（默认 configs/split.yaml 存在即读）→ 透传 V9/V10 参数 |
| `configs/split.yaml` (M) | +`testc_holdout: object_replacement` |
| `scripts/b3_preflight.py` (C) | HEAD 断言/净树断言/磁盘余量断言（参数化，供 B3 驱动起跑前调用） |
| `src/forgery_pipeline/rundir.py` (C) | `append_jsonl_fsync` / `mark_done` / `is_done` / `detect_incomplete_tail`（截断残尾+备份） |
| `docs/PREREG_gate2_v3_draft.md` (C) | 9.5 必备要素草案（占位符齐，锁定另行） |
| tests (C/M) | test_base_id.py / test_validate_v8_v10.py / test_b3_preflight.py / test_rundir.py |

---

### Task 1: base_id 字段全链落地 + 回填扩展

**Files:** Modify `src/forgery_pipeline/schema.py`、六个 builder、`pipeline.py`、`scripts/backfill_manifest_v7.py`；Create `tests/test_base_id.py`

**Interfaces (Produces):** `Sample.base_id: Optional[str]`；约定：D0 行 `base_id=image_id`；D2/D3/probe 各行 `base_id=base.image_id`；D1 行 `base_id=iid`（自身，全生成自成组）；D4 行继承源行 base_id；vae_rt 行继承源行 base_id；postprocess 行经 `model_copy` 自动继承。`backfill(in_path, out_path)` 额外回填 base_id。

- [ ] **Step 1: 失败测试**

```python
# tests/test_base_id.py
from forgery_pipeline.config import GeneratorSpec, PipelineConfig, StageScales
from forgery_pipeline.pipeline import run_pipeline
from forgery_pipeline import manifest
from scripts.backfill_manifest_v7 import backfill

def test_pipeline_rows_all_carry_base_id_and_groups_are_consistent(tmp_path):
    cfg = PipelineConfig(out_dir=str(tmp_path / "run"), seed=0, backend="mock",
                         stages={"d0": True, "d1": True, "d2": True, "d3": True,
                                 "d4": True, "postprocess": True, "split": True},
                         scales=StageScales(d0=12, d1_per_generator=1, d2=6, d3=4, d4=2),
                         generators=[GeneratorSpec("g1", "gan", "whole")],
                         inpainters=[GeneratorSpec("i1", "diffusion", "inpaint")],
                         vae_rt_frac=0.3)
    run_pipeline(cfg)
    rows = manifest.read_jsonl(tmp_path / "run" / "manifest.jsonl")
    assert all(r.base_id for r in rows)                      # 全行必填
    real_ids = {r.image_id for r in rows if r.sample_kind == "real"}
    for r in rows:
        if r.sample_kind == "real":
            assert r.base_id == r.image_id                   # D0 自指
        if r.sample_kind == "real_vae_rt":
            assert r.base_id in real_ids                     # 继承源
    # 同 base_id 组内 split 一致（V8 的数据前提）
    by_base = {}
    for r in rows:
        by_base.setdefault(r.base_id, set()).add(r.split)
    assert all(len(s) == 1 for s in by_base.values())

def test_backfill_fills_base_id(tmp_path):
    from forgery_pipeline.schema import Sample, TaskType
    rows = [Sample(image_id="r0", image_path="D0/r0.png", is_fake=0,
                   task_type=TaskType.real_pristine),
            Sample(image_id="f0", image_path="a.png", real_image_path="D0/r0.png",
                   is_fake=1, mask_path="m.png", manipulation_level1="partial_manipulated",
                   task_type=TaskType.localization)]
    p = tmp_path / "old.jsonl"; manifest.write_jsonl(p, rows)
    out = tmp_path / "new.jsonl"; backfill(p, out)
    r = manifest.read_jsonl(out)
    assert r[0].base_id == "r0" and r[1].base_id == "r0"     # 衍生行映射到 real 行 image_id
```

- [ ] **Step 2: 跑红**（AttributeError: base_id）。
- [ ] **Step 3: 实现** schema 在 `pair_id` 后加 `base_id: Optional[str] = None  # 底图组键（V8 split 互斥用；D0=自身 image_id，衍生行=底图 image_id）`。各 builder 的 Sample(...) 加 `base_id=...`（d0: iid；d1: iid；d2/d3/probe strength/operator/pairs: base.image_id；d4: src.base_id 或 src.image_id 兜底）。pipeline vae_rt 块：`v.base_id = s.base_id or s.image_id`。backfill：先建 `{image_path: image_id}`（is_fake==0 行），再逐行 `r.base_id = r.base_id or (r.image_id if not r.real_image_path and r.is_fake == 0 else mapping.get(r.real_image_path, r.real_image_path) or r.image_id)`。
- [ ] **Step 4: 跑绿 + 全套**（e2e/V 相关既有测试不应受影响——base_id 是新增可选字段）。
- [ ] **Step 5: Commit** `feat(schema): base_id 底图组键全链落地 + 回填扩展（PATCH 9.3 前置）`

### Task 2: V8–V10 + CLI --split-config + testc_holdout 写死

**Files:** Modify `src/forgery_pipeline/validate.py`、`src/forgery_pipeline/cli.py`、`configs/split.yaml`；Create `tests/test_validate_v8_v10.py`

**Interfaces (Produces):** `check_all(samples, profile="auto", vae_rt_range=(0.05,0.35), min_real=10, holdout_generators=None, testc_holdout=None)`；V8 无参常开（base_id 存在的行），V9 需 holdout_generators（set[str]，匹配 generator_name 或 generator_family），V10 需 testc_holdout（str，匹配 operator）；None 即跳过。消息前缀 "V8: "/"V9: "/"V10: "。

- [ ] **Step 1: 失败测试**（注毒负例逐条）

```python
# tests/test_validate_v8_v10.py 结构（helper 沿用 test_validate_v7 的构造惯例）
def test_v8_same_base_id_across_splits_fails(): ...       # 两行同 base_id 不同 split → "V8: "
def test_v8_postprocess_row_split_mismatch_fails(): ...   # postprocess_of 指向母行、split 不同 → "V8: "
def test_v8_green_when_groups_consistent(): ...
def test_v9_holdout_generator_in_train_fails(): ...       # generator_name 命中 holdout 且 split=train → "V9: "
def test_v9_holdout_family_in_val_fails(): ...            # generator_family 命中 → "V9: "
def test_v9_skipped_when_param_none(): ...
def test_v10_testc_holdout_operator_in_train_fails(): ... # operator==testc_holdout 且 split∈{train,val} → "V10: "
def test_v10_allowed_in_test_c(): ...
def test_v10_skipped_when_param_none(): ...
```

- [ ] **Step 2: 跑红。**
- [ ] **Step 3: 实现** validate.py 三个 check + check_all 接线；V8：按 base_id 分组断言 split 集合大小 1（打印组键+splits）+ postprocess_of 与母行 split 一致（按 image_id 建索引，母行缺失跳过）；V9：`(s.generator_name in hold) or (s.generator_family in hold)` 且 `s.split in {"train","val"}` → FAIL；V10：`s.operator == testc_holdout and s.split in {"train","val"}` → FAIL。cli.py：`p_val.add_argument("--split-config", default="configs/split.yaml")`；`_cmd_validate` 读 yaml（存在才读）：`hold=set(rules.get("holdout_generators", []))`、`tc=rules.get("testc_holdout")` → 传 check_all。configs/split.yaml 追加：`testc_holdout: object_replacement  # PATCH 8.3 几何探针裁定（2026-07-16），Test-C holdout 唯一 config 源`。
- [ ] **Step 4: 跑绿 + 全套**（注意 e2e：probe 的 test_b 行 generator=kandinsky 系 holdout——e2e 是 run_probe 产物且 split=test_b 合法，V9 只罚 train/val，应绿；若 e2e 用 profile=run 且 split.yaml 未传则 V9 本就跳过）。
- [ ] **Step 5: Commit** `feat(validate): V8–V10 split 防泄漏（注毒负例）+ --split-config + testc_holdout 写死`

### Task 3: b3_preflight + rundir 幂等原语

**Files:** Create `scripts/b3_preflight.py`、`src/forgery_pipeline/rundir.py`、`tests/test_b3_preflight.py`、`tests/test_rundir.py`

**Interfaces (Produces):** `b3_preflight.preflight(expected_head: str | None, min_free_gb: float, repo_root=".", data_dir=None) -> list[str]`（空=通过；检查 ①`git rev-parse HEAD` 前缀匹配 expected_head ②`git status --porcelain` 为空 ③data_dir（默认 repo_root）所在盘余量 ≥ min_free_gb）+ CLI（`--expected-head/--min-free-gb/--data-dir`，exit 0/1）。`rundir.append_jsonl_fsync(path, obj)`（open "a"+json 行+flush+fsync）；`rundir.mark_done(run_dir, cell_key)` / `is_done(run_dir, cell_key)`（`_done/<sha1(cell_key)>.marker` 文件）；`rundir.detect_incomplete_tail(path) -> bool`（末行 json 不可解析 → 备份原文件为 `.bak.<ts>` 后截掉残尾，返回 True；`ts` 由调用方传入或用行数，**不用 time**——保持确定性测试）。

- [ ] **Step 1: 失败测试**

```python
# tests/test_rundir.py
def test_append_and_reread_roundtrip(tmp_path): ...        # 追加 3 行→读回 3 行
def test_done_marker_roundtrip(tmp_path): ...              # mark→is_done True；未 mark False；key 含特殊字符安全（hash）
def test_detect_incomplete_tail_truncates_and_backs_up(tmp_path):
    # 手写 2 完整行 + 1 半行 → detect 返回 True、文件剩 2 行、.bak 存在且含原 3 段
def test_detect_clean_file_noop(tmp_path): ...
# tests/test_b3_preflight.py（在 tmp git repo 里演练）
def test_preflight_head_mismatch_fails(tmp_git_repo): ...  # expected_head="0000000" → 消息含 HEAD
def test_preflight_dirty_tree_fails(tmp_git_repo): ...     # 写脏文件 → 消息含 净树
def test_preflight_disk_threshold(tmp_git_repo): ...       # min_free_gb=10**6 → FAIL；=0 → 通过
def test_preflight_all_green(tmp_git_repo): ...
```

tmp_git_repo fixture：tmp_path 里 `git init` + 一次 commit（subprocess，env 里设 user.email/name），返回路径。

- [ ] **Step 2: 跑红。** — [ ] **Step 3: 实现**（subprocess git；磁盘用 `shutil.disk_usage`；注释注明 9.4 事故 A/B/C 对应关系与"驱动内禁评估"公约——preflight docstring 写明该纪律，供 B3 驱动 import 时同时当文档）。 — [ ] **Step 4: 跑绿 + 全套。**
- [ ] **Step 5: Commit** `feat(rundir): B3 驱动加固工具层——preflight 断言 + fsync 追加/done-marker/残尾检测`

### Task 4: PREREG_gate2_v3 草案 + 文档同步

**Files:** Create `docs/PREREG_gate2_v3_draft.md`；Modify `docs/PATCHES.md`、`docs/paper_experiment_plan_2026-07-15.md`、`docs/GATE_DATA.md`

- [ ] **Step 1:** 草案按 9.5 必备要素成文（以 v2 为模板）：状态=草案（锁定条件=评估前 commit 填毕版）；判据主 same-model BA≥0.50 / cross-model≥0.40（注明 K 与机会线）、cluster CI（按 base_id）、阈值不因 gate1 调整；方向性预言（空间头 vs 全局幅值 probe，两估计器冻结条款占位）；null baseline=掩码几何 probe（复用 checking/testc_geometry_probe.py）配对 Δ CI 下界>0；纤维化协议沿用 gate1 模板；verdict 三档落点表（全量归因/算子族/坍缩为定位）；失效清单沿用 v2；占位符表（P1 特征 commit/P2 cell 网格/P3 面积桶界/P4 nuisance 采样声明）。
- [ ] **Step 2:** PATCHES.md 追加 "PATCH 9 Wave 1 ✅（9.3 全部 + 9.4 工具层 + 9.5 草案；9.1/9.2/9.5 冻结/9.6 另行）"；experiment plan B1.6 的 9.3 打勾并注 V8–V10 落地、9.4 注"工具层就绪、驱动本体随 B3"；GATE_DATA 字段表 += `base_id` 一行。
- [ ] **Step 3:** 全套 pytest 守门（docs 不该动测试）→ Commit `docs(patch9): wave1 记录 + PREREG_gate2_v3 草案 + base_id 字段登记`

---

## Self-Review 结论

- 覆盖：9.3 全部（base_id/V8/V9/V10/注毒/回填）✓；9.4 的可本地部分（HEAD/净树/磁盘断言、幂等原语、评估禁令以 docstring 公约固化）✓——latent 复用与 COCO fetch 归 GPU/B2 侧，明示不在本 wave；9.5 草案 ✓（冻结与锁定另行）；9.1/9.2/9.6 明示出界。
- 决策已内嵌：D1 全生成行 base_id=自身（自成组，V8 不空转不误伤）；V9 罚 train/val 而非"非 test_b 全部"（val 同样必须干净）；detect_incomplete_tail 不用系统时间（确定性）。
- 类型一致：check_all 新参默认 None 向后兼容（V1–V7 测试零改动）。
